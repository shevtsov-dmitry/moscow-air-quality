package application.admin_part.admin;

import application.admin_part.admin.Authorization;

public class VerifyAdmin extends Authorization {
    Admin admin = new Admin();
    @Override
    public boolean verify() {
        return super.verify();
    }

}
